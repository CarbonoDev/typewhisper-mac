import SwiftUI

/// Retained as an inert stub. TypeWhisper is free and open source; the first-run
/// usage-intent / commercial-license welcome sheet has been removed and this view is
/// never presented.
struct WelcomeSheet: View {
    var body: some View {
        EmptyView()
    }
}
